# Configuración del servidor
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Credenciales de MongoDB
# Usando la cadena de conexión de MongoDB Atlas que proporcionaste:
MONGODB_URI=mongodb+srv://Gogeta:Rodri2008@cluster0.uifbnkp.mongodb.net/marketplace?appName=Cluster0

# Se recomienda incluir el nombre de la base de datos (e.g., 'marketplace') en la URI para mayor claridad.
# El resto de la URI ya tiene ?appName=Cluster0.

# Secreto de JWT (JSON Web Token)
# ¡IMPORTANTE! Genera una clave más compleja para producción.
JWT_SECRET=hola
JWT_EXPIRE=7d